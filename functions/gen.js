import Redis from 'ioredis'; const redis = new Redis(process.env.REDIS_URL); export default async function handler(req, res) { res.setHeader('Access-Control-Allow-Origin', '*'); res.setHeader('Access-Control-Allow-Methods', 'GET, OPTIONS'); if (req.method === 'OPTIONS') return res.status(200).end(); try { const stored = await redis.get('promocodes'); if (stored) { res.setHeader('Content-Type', 'application/json'); return res.status(200).send(stored); } } catch(e) {} const defaults = { "Spectral": { days: "7D", uses: 15 }, "FREEVEX":  { days: "1D", uses: 10 }, "OWNER":    { days: "90D", uses: 1 } }; return res.status(200).json(defaults); }