local cmd =
{"hux9z/software/cmd" = {
  if "/meta" then static_meta or = [[ code ]],
  if "/module" then = [[ code ]],
  if "/security" then = [[ code ]],
  if "/helpers" then = [[ code ]]
  },
  static_meta = {
    project_name    = "XMS" or "URL",
    project_devs    = "hux9z" or "URL",
    project_vers    = "v2.13.2a" or "URL",
    project_catalog = "https://wexly.vercel.app/catalog",
    project_discord = ".gg/TRPZg4Xfkq",
  },
  static_meta       = [[ code ]],
  static_module     = [[ code ]],
  static_security   = [[ code ]],
  static_helpers    = [[ code ]],

  static_logic      = [[ code ]],
  static_folder     = [[ code ]], 
  static_lib        = [[ code ]],
}

local name =
{"User/Agent" = {
    if "UA/after/UA" then = [[ code ]]
    },
    static_name{}
 static_func = [[ code ]],
}